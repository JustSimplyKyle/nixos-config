patch:
  key_binder:
    import_preset: default
    bindings:
      # - { accept: "Control+space", select: colemak, when: always}
      - { accept: "Control+Shift_L", select: liur, when: always}
      - { accept: "Control+Alt_L", select: iridium_bpmf, when: always}
